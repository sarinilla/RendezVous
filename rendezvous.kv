DeckDraw:
    Label:
        text: "Lovers & Spies"


<CardDisplay>:
    canvas:
        Rectangle:
            texture: root.texture
            size: dp(200), dp(280)
            pos: root.pos[0]-dp(100), root.pos[1]-dp(140)